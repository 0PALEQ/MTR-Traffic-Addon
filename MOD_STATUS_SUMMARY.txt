MTR Traffic Addon - Status Summary
Updated: 2026-05-21
Version covered: 26.5.A04

Current working state
=====================

- The mod builds successfully as a Fabric 1.20.1 addon for Minecraft Transit Railway 4.0.4.
- The current confirmed build output is:
  D:\mta\build\libs\mtr-traffic-addon-26.5.A04.jar
- The current standalone sedan resource pack export is:
  D:\mta\resourcepacks\mtr-traffic-addon-sedan.zip
- The default Java environment on this machine may still point to Java 17, while the current Fabric Loom setup needs Java 21 or newer.
- The latest verified build used JDK 23:
  JAVA_HOME=C:\Program Files\Java\jdk-23
  .\gradlew.bat build --no-daemon
- In-game testing confirmed that the new sedan vehicles render, use the expected orientation, and no longer drive backwards.
- In-game testing confirmed that the new Auto/Manual traffic signal mode work is usable, with follow-up fixes applied for yellow/all-red transitions and stuck stop-line vehicles.

26.5.A04 changes since 26.5.A03
===============================

- Updated project version output from 26.5.A03 to 26.5.A04.
- Added traffic light signal modes:
  - Manual
  - Auto
- Manual mode keeps the previous phase-order based traffic light behavior.
- Auto mode detects waiting vehicles before IN nodes assigned to an intersection.
- Auto mode queues the group containing the demanded IN node.
- Auto mode switches the currently green group to red after a delay when another group is waiting.
- Auto mode waits for the intersection to clear before giving green to the next queued group.
- Auto mode keeps the current group green while no other group has demand.
- Green light duration now has a minimum of 15 seconds.
- A green phase can end early if the current group has no cars left and another demanded group is queued.
- Yellow/all-red clearance is applied before giving green to the next group, even when the current group is already empty.
- Fixed unrealistic instant green-to-green changes when an empty group hands off to another group.
- Fixed a case where vehicles could stop permanently because a vehicle standing near an IN node or stop line was treated as blocking intersection clearance forever.
- Added dashboard/network snapshot support for the signal mode value.
- Added dashboard controls for switching an intersection between Manual and Auto.
- Updated intersection signal timing text and dashboard state to reflect the newer signal behavior.
- Added a signal clock based on wall time/server tick synchronization so signal checks remain stable during pauses or tick gaps.
- Limited stale MTA traffic vehicle obstacle checks after long pauses so old positions do not indefinitely block MTR route vehicles.

Sedan asset replacement
=======================

- Removed the old broken sedan resource set from the embedded mod assets and resourcepack source copy.
- Replaced the sedan with the supplied mesh assets.
- Added the supplied OBJ mesh as the actual traffic rendering model:
  assets/mtr_traffic_addon_sedan/models/vehicle/sedan.obj
- Kept the supplied BBModel copy for resource metadata/future compatibility:
  assets/mtr_traffic_addon_sedan/models/vehicle/sedan.bbmodel
- Rebuilt the sedan resource registration under:
  assets/mtr/mtr_custom_resources.json
- Added eight sedan visual IDs:
  - mta_sedan
  - mta_sedan_white
  - mta_sedan_black
  - mta_sedan_green
  - mta_sedan_red
  - mta_sedan_blue
  - mta_sedan_brown
  - mta_sedan_orange
- Added eight matching traffic vehicle definitions:
  - sedan_01
  - sedan_white
  - sedan_black
  - sedan_green
  - sedan_red
  - sedan_blue
  - sedan_brown
  - sedan_orange
- Added eight 256x256 texture variants:
  - sedan.png
  - sedan_white.png
  - sedan_black.png
  - sedan_green.png
  - sedan_red.png
  - sedan_blue.png
  - sedan_brown.png
  - sedan_orange.png
- Updated the standalone resourcepack copy under:
  resourcepacks/mtr-traffic-addon-sedan
- Regenerated:
  resourcepacks/mtr-traffic-addon-sedan.zip

Custom model rendering changes
==============================

- Enabled the custom traffic model registry on the client.
- Changed traffic rendering to try the custom traffic model renderer first, then fall back to the MTR vehicle resource renderer.
- Added OBJ traffic model definitions under:
  assets/mtr_traffic_addon_sedan/traffic_models/*.json
- Fixed the supplied mesh not rendering through the MTR BBModel renderer by using the existing OBJ renderer path.
- Fixed OBJ triangle rendering with Minecraft quad-based RenderType output by emitting triangle faces as degenerate quads.
- Removed forced FULL_BRIGHT lighting from the custom OBJ renderer.
- Custom traffic models now sample world lighting with LevelRenderer.getLightColor at the vehicle position.
- Fixed sedan orientation by changing the OBJ model rotation from +90 degrees to -90 degrees around Y.
- Added safer fallback behavior if a custom model render fails.

Documentation updates
=====================

- Updated README.md to list all current built-in sedan visual IDs and traffic vehicle definitions.
- Updated CUSTOM_TRAFFIC_MODELS.md to describe the OBJ-based sedan rendering path.
- Updated docs/RESOURCE_PACK_AUTHORING.md with the current sedan variants and resource authoring notes.
- Updated resourcepacks/mtr-traffic-addon-sedan/README.md to describe the current exported resourcepack contents.

Build verification
==================

Last verified command:

JAVA_HOME=C:\Program Files\Java\jdk-23
.\gradlew.bat build --no-daemon

Result:

BUILD SUCCESSFUL

Current jar:

D:\mta\build\libs\mtr-traffic-addon-26.5.A04.jar

Current resourcepack export:

D:\mta\resourcepacks\mtr-traffic-addon-sedan.zip

Known notes and remaining work
==============================

- The default Java on this machine should be checked before release builds; Fabric Loom requires Java 21+.
- Auto signal behavior has been tested interactively, but larger intersections with many groups should still be tested in-game.
- The new sedan mesh now renders correctly in-game after the OBJ renderer fixes, but other future mesh formats may need separate loader support.
- The external resourcepack copy should not be enabled alongside the mod jar unless intentionally overriding embedded resources.
- Existing worlds or spawn connector pools that referenced removed/old sedan IDs should be checked and refreshed if needed.
